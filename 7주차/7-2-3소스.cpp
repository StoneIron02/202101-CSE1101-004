/*#include <iostream>
using namespace std;
int sum(int a, int b)
{
	cout << "the area is : " << a * b << endl;
	return 0;
}
int sum(int a)
{
	cout << "the area is : " << a * a << "ㅠ" << endl;
	return 0;
}
float sum(float a, float b)
{
	cout << "the area is : " << a * b/2 << endl;
	return 0;
}
int main()
{
	int a;
	int b;
	cout << "1 - 직사각형, 2 - 원, 3 - 삼각형 : ";
	int input;
	cin >> input;
	switch (input)
	{
	case 1:
		cout << "you are select rectangle, please input two integar for length and height : ";
		cin >> a >> b;
		sum(a, b);
		break;
	case 2:
		cout << "you are select rectangle, please input two integar for length and height : ";
		cin >> a;
		sum(a);
		break;
	case 3:
		float c;
		float d;
		cout << "you are select rectangle, please input two integar for length and height : ";
		cin >> c >> d;
		sum(c, d);
		break;
	}
}*/