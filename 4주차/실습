#include<iostream>;
#include<iomanip> ;
using namespace std;

int main() {
	/*
	int star;     //q1
	int num;
	cin >> num;
	int line;
	line = 1;
	star = 1;
	while (line <= num) {

		while (star <= line) {
			cout << "*";
			star++;
		}
		star = 1;
		line = line + 1;
		cout << endl;
	}
	line = num - 1;
	while (line >= 1) {
		star = line;
		while (star >= 1) {
			cout << "*";
			star--;
		}
		line--;
		cout << endl;
	}*/

	
	
	/*
	int result;              //q2
	int num;
	cout << "insert number";
	cin >> num;
	result = 1;
	for (int i(1); i <= num; i++) {
		result = result * i;
	}
	cout << num << "! =" << result; */


	/*	int n, m;      //q3
		cout << "insert N,M ";
		cin >> n >> m;
		for (int x = 1; x <= n; x = x + m) {
			for (int i = 1; i <= n; i++) {
				for (int z = x; z <= n; z++) {
					cout << z << " * " << i << " = " << z * i << "\t";
					if (z % m == 0)
						break;
				}
				cout << endl;

			}
			cout << endl;
		}   */       
		
	
	/*
	int n, m,total,a ;               //q4
	total = 0;
	cout << "insert n,m";
	cin >> n >> m;
	for (int i = 1; i <= 100; i++) {
		if (i % n == 0 || i % m == 0) {
			total++;
			cout << i<<"  ";
		}
		if (total% 5 == 0)
			cout << endl;
	}
	cout<<endl << "total of number is " << total;*/
	

	/*
	int atotal , btotal;       //q5
	char ch;
	atotal = 0;
	btotal = 0;
	while ((ch = cin.get()) != EOF)
		switch (ch) {

		case 'a':
		case 'e':
		case 'i':
		case 'o':
		case 'u':
		case 'A':
		case 'E':
		case 'I':
		case 'O':
		case 'U':
			atotal++;
		default:
			btotal++;
		}
	cout << "모음은 " << atotal << "개, " << "자음은 " << btotal << "개 있습니다.";*/
		 

	
	
	return 0;
}

